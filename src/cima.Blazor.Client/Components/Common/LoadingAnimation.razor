<div class="flex flex-col items-center justify-center p-8">
    <svg width="100" height="100" viewBox="0 0 200 200" class="cima-cad-animation text-navy-700">
        <path d="M 50 100 L 100 50 L 150 100" fill="none" stroke="currentColor" stroke-width="2" />
        <rect x="60" y="100" width="80" height="60" fill="none" stroke="currentColor" stroke-width="2" />
        <rect x="85" y="120" width="30" height="40" fill="none" stroke="currentColor" stroke-width="2" />
    </svg>
    <p class="mt-4 text-navy-700 font-display animate-pulse">Cargando...</p>
</div>

<style>
    .cima-cad-animation path, .cima-cad-animation rect {
        animation: draw 2s infinite;
    }

    @@keyframes draw {
        0% {
            stroke-dasharray: 1000;
            stroke-dashoffset: 1000;
        }
        50% {
            stroke-dashoffset: 0;
        }
        100% {
            stroke-dashoffset: 0;
            opacity: 0;
        }
    }
</style>

@code {
    // Componente estático
}
