namespace cima.Domain.Shared
{
    public enum PropertyStatus
    {
        Draft = 0,
        Published = 1,
        Archived = 2
    }

    public enum ContactRequestStatus
    {
        New = 0,
        Replied = 1,
        Closed = 2
    }
}
